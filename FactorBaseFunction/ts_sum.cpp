#include <iostream>
#include <vector>
#include <Rcpp.h>
using namespace Rcpp;

NumericVector ts_sum(
    Rcpp::NumericVector x,
    const int window,
    Rcpp::NumericVector weight,
    const bool intercept,
    const bool sign,
    const bool skip,
    const bool partial,
    const int least,
    const bool fill,
    const int threads,
    const int last, 
)
{

}

int main()
{

    return 0;
}